"use client";

/**
 * Framer Motion entry for the App Router: import from here in **client components** only
 * (`"use client"` at the top of the file that uses motion).
 */
export {
  AnimatePresence,
  motion,
  useReducedMotion,
  useScroll,
  useSpring,
  useTransform,
} from "framer-motion";
